# RINEX Mapping

The signal mapping is intentionally explicit and conservative. Where UM980
tracking status fields are not yet decoded, observation rows keep the raw
tracking status so mappings can be verified against receiver documentation and
real captures before being promoted to production defaults.

